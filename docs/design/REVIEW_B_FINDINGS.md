# REVIEW-B-FINDINGS — Packet B adversarial review (tech / UX / benchmark / process / privacy / performance)

Reviewer: fresh opencode session, first contact with the repo, no author context. Method: read the 15 packet files in full; cross-checked SCIENCE_MODEL.md and EVIDENCE.md; executed `node docs/design/evidence/calibration-sim.mjs` (reports exactly 27/45 pass — matches EVIDENCE §5 and DECISIONS D-31); inspected the actual sibling repos (`game-math-detective`/`bridge-builder`/`number-line-jumper` package.json + dist) and `games-site/docs/game-release-contract.md` on this host; verified Phaser 4.2.1 (released 2026-07-09) and React 19.3 (2026-09-09, features as ADR-3 describes) against the public record.

Verified clean (no finding): EVIDENCE §1–§2 sibling pins and payload bytes are real and exact (phaser.esm 1,375,290 B raw / ~346 KB gz confirmed in `game-math-detective/dist`); EVIDENCE §3 games-site contract claims match the actual contract doc; ACCEPTANCE §9 release checklist is consistent with it; the calibration harness honestly reports its own failures; tech-stack ADRs decline more than they add (no overengineering found); no claim in my scope tested false. Packet-A ground (harness red, provisional values) is not re-litigated here.

**No BLOCKER-level finding.** Four MAJOR, six MINOR, four QUESTION:

[MAJOR] PERFORMANCE_BUDGETS §3–§5 ??measurement methods cannot validate the thresholds they enforce. Evidence: §3 sets cold-start ≤6.0 s (R1) "On R1 CPU + 10 Mbps" but the method is "Lighthouse (Slow 4G/4× CPU throttle)" — Slow 4G is ~1.6 Mbps/150 ms RTT, so at the bundle release limits (≈1.24 MB first visit) transfer alone is ~6.3 s: CI false-fails compliant builds, and the doc's own "1 Mbps worst case (10–12 s)" row has no threshold; likewise §4's "R1-class VM (CI)" and §5's Vitest "R1" kernel budgets are never defined against the 2018-Celeron reference (GitHub runners are not R1-class; no throttling calibration is specified). Why it matters: ER-02 wires gates that false-fail or pass on the wrong hardware; ER-12 gate 8 ("budgets pass") is not objectively checkable as written.

[MAJOR] UX_USER_FLOW §9/§11 + ACCEPTANCE_CONTRACT §6.1 ??claims "WCAG 2.2 AA" (UX header, QUALITY_BENCHMARK dim 9, D-22) but the frozen evidence envelope is 360 px + 200% zoom, while WCAG 2.2 AA SC 1.4.10 Reflow requires no two-dimensional scrolling at 320 CSS px (= 400% zoom on a 1280 px viewport). Evidence: UX §11's smallest defined layout is 360–639 px; ACCEPTANCE §6.1 CI fixtures are "200% reflow fixture, 360 px layout fixture"; no 320 px/400% fixture or budget exists anywhere in the package. Why it matters: dimension 9 is release-blocking; a real audit at the actual AA bar could fail a build that passed every frozen check — the conformance claim exceeds what the evidence protocol can establish.

[MAJOR] COMPARATOR_RUBRIC §3.9 vs ACCEPTANCE_CONTRACT §6.2 vs UX_USER_FLOW §9 ??three different screen-reader requirements for one human gate: rubric (the frozen instrument ER-14 "must use") says "NVDA or VoiceOver"; the contract says "NVDA + VoiceOver at minimum"; UX adds JAWS. Evidence: all three quoted verbatim; the rubric's dim-9 "meets" observable is the weakest of the three. Why it matters: two reviewers following the frozen form can produce single-SR evidence that formally violates the contract's minimum for a release-blocking dimension.

[MAJOR] ACCEPTANCE_CONTRACT §2/§3.5 (ER-15) + TECHNICAL_DESIGN §D-11 ??the only architecture element that emits learner data off-page (HostFacts "completion/summary emissions"; HostContext "session" context) carries no privacy evidence requirement in its gate chain. Evidence: ER-15's evidence list is solely "host contract conformance tests; standalone parity"; ADR-6's privacy invalidation covers only "cross-device progress"; no document requires a privacy review when host mode activates. Why it matters: the headline posture ("not sent anywhere; no identifiers"; ADR-6 "eliminates … COPPA-adjacent data concerns entirely") is defined for standalone only; ER-15 as written can ship a live outbound learner-data channel with zero privacy gate, and nothing binds the later activation to review.

[MINOR] ACCEPTANCE_CONTRACT §4 gate 12 ??"When: ER-PROMOTE" names a story that does not exist. Evidence: repo-wide grep finds exactly one occurrence (ACCEPTANCE_CONTRACT.md line 81); the §2 inheritance table runs ER-02…ER-16; ER-13 already owns "rollback rehearsal documented". Why it matters: a release-gate row with an unresolvable owner — the next engineer cannot tell which story executes "rollback exercised".

[MINOR] TECHNICAL_DESIGN/SCIENCE_MODEL/EVIDENCE/COMPARATOR_RUBRIC ??systematic cross-reference rot in the frozen governing package. Evidence: TECHNICAL_DESIGN D-4 (line 65) and D-6 (line 80) point to §D-9 for failure behavior (the matrix is §D-8); SCIENCE_MODEL R-52 cites "TECHNICAL_DESIGN §D-6" for NaN/∞ serializer rejection (defined in §D-3.4); R-50 cites "TECH_STACK_ADR §7" for the PRNG choice (that ADR is state management; the PRNG decision lives in TECHNICAL_DESIGN §D-2, with no ADR); EVIDENCE §3 cites "TECHNICAL_DESIGN §D-10/§D-8" for release-format alignment; COMPARATOR_RUBRIC §3.10 cites "PERFORMANCE_BUDGETS §7 measurement sheets" (the sheets are §3–§6). Why it matters: README claims "they are all here" and forbids scope invention; wrong pointers in a frozen contract force implementers to guess which text is normative.

[MINOR] docs/design/README.md docs table ??"Decision ledger D-01…D-29" is stale. Evidence: DECISIONS.md contains D-01…D-31 (D-30/D-31 added by the calibration commits, D-31's values explicitly provisional). Why it matters: the package index misstates the ledger it indexes, and "no release-blocking TBDs" sits awkwardly beside D-31's "values provisional / harness fails 18 of 45".

[MINOR] GDD §8 vs TECHNICAL_DESIGN §D-7 ??GDD promises "remember-me is per-tab session storage" (condensed briefing for returning players), but D-7's frozen allowlist ("nothing else": settings, mission snapshot, conclusion summaries) has no returning-player/briefing-seen field. Evidence: both quoted verbatim; deriving it from summary-presence fails for a player who quit M1 mid-way. Why it matters: ER-02's privacy guards enforce the D-7 allowlist; ER-05 must either violate it or silently reinterpret it — a hidden dependency between frozen docs.

[MINOR] TECH_STACK_ADR ADR-5 vs QUALITY_BENCHMARK §2 dim 3 ??ADR-5's revisit hook ("visual-quality parity failure vs the Terra Nil data-presentation target") keys on a benchmark the frozen matrix declines: dim 3 (evidence/data readability) vs Terra Nil is INC. Evidence: QUALITY_BENCHMARK §2 row 3 = "Exceed | INC | Parity". Why it matters: an architecture-invalidation trigger referenced to a nonexistent target is unexecutable; Terra Nil's actual targets are dims 6 (Exceed) and 12 (intentionally below).

[MINOR] COMPARATOR_RUBRIC §2 ??the frozen form's target vocabulary is "parity / exceed / INC (from QUALITY_BENCHMARK §2)", but dims 11 ("Parity (at our scale)") and 12 ("Intentionally below Terra Nil's AAA polish, above minimum") are none of these. Evidence: rubric §2 form field vs QUALITY_BENCHMARK §2 rows 11–12. Why it matters: the ER-14 reviewer copying the frozen form cannot fill "Frozen target" for two dimensions — "meets" is undefined for them as instrumented.

[QUESTION] ACCEPTANCE_CONTRACT §4 gates 3/4/13 + §6.2 ??the human-evidence pipeline for minors is unspecified. Evidence: gates require ≥5 learners, recorded sessions, playtest notes, reviewer identities, yet no document states consent/parental permission, what media is recorded, where it is stored, or retention; D-7/ADR-6 cover only the learner runtime, and EVIDENCE §5/D-29 record that no human reviewer was even available in this environment. Why it matters: ER-14's class-B evidence about children is collected with no data-handling rules and no sourcing plan.

[QUESTION] UX_USER_FLOW §10 / TECHNICAL_DESIGN §D-8 ??the fatal shell's "Copy diagnostic details" block has no content spec. Evidence: D-8 says only "curated, learner-safe strings + a technical detail block for adults/bug reports"; D-7's allowlist is not referenced for the copied payload. Why it matters: if the technical block embeds the session snapshot (the obvious debug payload), a pasted bug report exports the learner's ≤200-char hypothesis nuance off-device — state the diagnostic allowlist.

[QUESTION] PERFORMANCE_BUDGETS §3 ??~60 KB gz of JS headroom (500 − ~440 sibling) is assumed to cover the entire evidence/mission layer. Evidence: §3 note "60 KB headroom"; measured sibling app chunk is 88 KB gz including React; this game adds a six-tab semantic panel, SVG charts, food web + tables, notebook, conclusion builder, mission machine, scenario schema, host seam, persistence. Why it matters: if ER-05–ER-09 land at sibling density on this larger surface, the release threshold breaches mid-Epic, forcing a budget amendment or late refactor; no per-layer allocation or early canary exists.

[QUESTION] COMPARATOR_RUBRIC §2 ??"Comparator observation: … observed firsthand" requires two human reviewers to play three commercial titles (Eco is a paid multiplayer/server game; Tyto Ecology's availability is uncertain). Evidence: rubric §2 form + QUALITY_BENCHMARK §4.1/§4.4; no license/access provision or documented-footage fallback exists. Why it matters: the ER-14 instrument may be unexecutable as written for reviewers without the games, inviting fabricated "observations".

VERDICT: PASS-WITH-FIXES
